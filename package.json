{
  "name": "extbsv",
  "scripts": {
    "build": "[ -d build ] && rm -r build; browserify src/listenInject.js src/bsvConfigInject.js -o build/inject.js && cp src/{popup.html,popup.js,content.js} resources/icon.png manifest.json build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "bsv-better-service-virtualization": "0.0.8"
  },
  "devDependencies": {
    "browserify": "^16.1.0"
  }
}
